# Docker

Docker-related infrastructure will live here as service images are added.

TODO:

- Add gateway Dockerfile
- Add control API Dockerfile
- Add worker Dockerfile
- Add dashboard Dockerfile
- Add local NGINX origin example
